[package]
name = "autotest-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
uapi = { path = "../../../../uapi", default-features = false }

[features]
default = []

[profile.dev]
panic = "abort"
opt-level = "z"
